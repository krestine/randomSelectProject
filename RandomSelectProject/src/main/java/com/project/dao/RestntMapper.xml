<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.dao.RestntMapper">
	<!-- <resultMap type="com.project.domain.RestntDTO" id="RestntDTO"> <result 
		property="id" column="id" javaType="string" /> <result property="name" column="name" 
		javaType="string" /> <result property="latitude" column="latitude" javaType="string" 
		/> <result property="longitude" column="longitude" javaType="string" /> <result 
		property="phone" column="phone" javaType="string" /> <result property="address1" 
		column="address1" javaType="string" /> <result property="address2" column="address2" 
		javaType="string" /> <result property="address3" column="address3" javaType="string" 
		/> <result property="address4" column="address4" javaType="string" /> <result 
		property="type" column="type" javaType="string" /> </resultMap> -->


	<!-- 공통, 회원, 관리자 : 식당 전체 리스트 -->
	<select id="getRestntList" resultType="RestntDTO">
		select * from restnt
	</select>

	<!-- 공통, 회원, 관리자 : 식당 코드를 사용한 필터 적용 후 리스트 -->
	<select id="getRestntListByAddessCode" resultType="RestntDTO"
		parameterType="string">
		select restntName, restntId
		restntLatitude,restntLongitude,
		restntAddress1,restntAddress2,
		restntAddress3,restntAddress4;
		restntPhone,restntType from restnt where addressCode like '${addresscode}%'
	</select>

	<!-- 관리자 : 식당 삭제 -->
	<delete id="dropRestntByrestntId" parameterType="string">
		delete from
		restnt where id = #{restntId}
	</delete>

	<!-- 관리자 : 식당 수정 -->
	<update id="setRestntByrestntId" parameterType="RestntDTO">
		update restnt set
		restntName = #{restntName}, restntLatitude = #{restntLatitude},
		restntLongitude=#{restntLongitude}, restntPhone=#{restntPhone},
		restntType=#{restntType}, restntAddress1=#{restntAddress1},
		restntAddress2=#{restntAddress2}, restntAddress3=#{restntAddress3},
		restntAddress4=#{restntAddress4} where restntId = #{restntId}
	</update>

	<!--관리자 : 식당정보 추가 -->
	<insert id="putRestnt" parameterType="RestntDTO">
		insert into restnt
		(restntId, restntName, restntLatitude, res tntLonogitude,
		restntAddress1, restntAddress2, restntAddress3, restntAddress4,
		restntPhone, restntType), values (#{restntId}, #{restntName},
		#{restntLatitude}, #{restntLongitude}, #{restntAddress1},
		#{restntAddress2}, #{restntAddress3}, #{restntAddress4},
		#{restntPhone}, #{restntType})
	</insert>

	<!--관리자 : 식당관리 - 식당리스트에서 식당선택 - 상세정보 표시 -->
	<select id="getRestntInfoById" parameterType="RestntDTO"
		resultType="RestntDTO">
		select restntName, restntAddressCode
		restntLatitude,restntLongitude,
		restntAddress1,restntAddress2,
		restntAddress3,restntAddress4;
		restntPhone,restntType where restntId=#{restntId}
	</select>

</mapper>