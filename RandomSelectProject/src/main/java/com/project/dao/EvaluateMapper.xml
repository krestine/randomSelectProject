<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.dao.EvaluateMapper">
	<!--관리자 : 유저 아이디로 평가 목록 열람  -->
	<select id="getEvaluateListByUserId" parameterType="String" resultType="EvaluateDTO">
		select * from evaluate where userid = #{userId}
	</select>
	<!--회원:식당평가한 목록 -->
	<select id="getEvaluateListByMemId" parameterType="String"
		resultType="EvaluateDTO">
		select evaluateId, evaluateScore, evaluateDate, restntId  from Evaluate
		where memId=#{memId}
		select resntName where evaluate.restntId = restnt.restntId
	</select>

	<!-- 회원: 평가한 방문리스트 수정 -->
	<update id="setEvaluationInfoByEvaluationTerms" parameterType="EvaluateDTO">
		update Evaluate set evalScore =#{evalScore},
		where evalId=#{evalId}
	</update>

	<!-- 회원: 평가안한 방문리스트 -->
	<select id="getnEvaluateListByMemid" parameterType="EvaluateDTO"
		resultType="EvaluateDTO">
		select evaluateId, evaluateDate, RestntId from Evaluate
		where memId=#{memId}
		select restntName where evaluate.restntId=restnt.restntId
	<!--어떻게 구현하지???  -->
	</select>

<!-- 	 회원: 평가안한 방문리스트 입력 
	<update id="setEvaluationInfoByEvaluationTerms" parameterType="EvaluateDTO">
		update Evaluate set evalScore =#{evalScore},
		where evalId=#{evalId}
	</update> -->
	
	</mapper>
