<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-.-mapper.dtd">
<mapper namespace="ezo.mp.dao.UserMapper">



	<select id="getJobCode" resultType="Job">
		select * from jobcode
	</select>

	<insert id="insertUser" parameterType="Users">
		insert into users
		(userid,
		name, password, phonenumber, address, jobcode)
		values
		(#{userid},
		#{name}, #{password}, #{phonenumber}, #{address}, #{jobcode})
	</insert>

	<select id="loginUser"  resultType="Users" >
		
	<!-- 	select userid from Users where name=#{name}  and password=#{password}
			 -->
<!-- 			 select userid from User2 where name='홍성민'  and password='qqww' -->
			 	 select *  from Users where userid=#{userid}  and password=#{password}
	<!-- 	 select * from Users where userid= 'hsm' and password='qqww'
 -->	<!-- 	select password,userid from Users where password= #{password}
	 --></select>

</mapper>